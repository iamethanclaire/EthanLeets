class Solution:
    def finalValueAfterOperations(self, operations: List[str]) -> int:
        x = 0
        for index in operations:
            if index[1] == '-':
                x-=1
            elif index[1] == '+':
                x+=1
        return x
                 
