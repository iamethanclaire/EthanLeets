class Solution:
    def checkDivisibility(self, n: int) -> bool:
        digits = [int(digit) for digit in str(n)]
        sum = 0
        prod = 1
        for digit in digits:
            sum += digit
            prod *= digit
        return n % (sum + prod) == 0
