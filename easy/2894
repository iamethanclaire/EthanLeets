class Solution:
    def differenceOfSums(self, n: int, m: int) -> int:
        div = 0
        udiv = 0
        for i in range(n + 1):
            if(i%m == 0):
                udiv += i
            else:
                div += i
        return div - udiv
                
        
